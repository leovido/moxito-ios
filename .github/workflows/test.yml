name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: macos-14
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Select Xcode Version
      run: sudo xcode-select -s /Applications/Xcode_15.2.app
    
    - name: List available schemes # Debug step
      run: xcodebuild -workspace fc-poc-wf.xcworkspace -list
    
    - name: Build
      run: xcodebuild build-for-testing -workspace fc-poc-wf.xcworkspace -scheme Moxito-DEBUG -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=17.2'
    
    - name: Run tests
      run: |
        xcodebuild test-without-building \
          -scheme MoxitoTests \
          -destination 'platform=iOS Simulator,name=iPhone 15 Pro,OS=17.2' \
          -workspace fc-poc-wf.xcworkspace -quiet \
          -enableCodeCoverage YES \
          -resultBundlePath TestResults
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        xcode: true
        xcode_derived_data: ./DerivedData 